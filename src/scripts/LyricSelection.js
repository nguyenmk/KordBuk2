
export default class LyricSelection {
  constructor(lineStart, posStart, lineEnd, posEnd) {
    this.lineStart = lineStart;
    this.start = posStart;
    this.lineEnd = lineEnd;
    this.end = posEnd;
  }
}